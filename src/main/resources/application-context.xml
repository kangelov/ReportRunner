<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:context="http://www.springframework.org/schema/context"
       xmlns:mvc="http://www.springframework.org/schema/mvc"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xsi:schemaLocation="
        http://www.springframework.org/schema/beans
        http://www.springframework.org/schema/beans/spring-beans-4.0.xsd
        http://www.springframework.org/schema/context
        http://www.springframework.org/schema/context/spring-context-4.0.xsd
        http://www.springframework.org/schema/mvc
        http://www.springframework.org/schema/mvc/spring-mvc-4.0.xsd
">

    <!-- Spring MVC -->

    <context:component-scan base-package="com.qualicom.rr" />

    <bean class="org.springframework.web.servlet.view.InternalResourceViewResolver">
        <property name="viewClass" value="org.springframework.web.servlet.view.JstlView" />
        <property name="prefix" value="/WEB-INF/views/" />
        <property name="suffix" value=".jsp" />
    </bean>

    <mvc:annotation-driven />

    <!-- JDBC -->

    <context:property-placeholder location="classpath:application.properties"/>

    <bean id="tmpgarDataSource" class="org.apache.commons.dbcp.BasicDataSource" destroy-method="close">
        <property name="driverClassName" value="${jdbc.driverClassName}"/>
        <property name="url" value="${jdbc.url}"/>
        <property name="username" value="${jdbc.username}"/>
        <property name="password" value="${jdbc.password}"/>
        <property name="defaultReadOnly" value="true" />
    </bean>

    <bean id="reportDao" class="com.qualicom.rr.dao.impl.ReportDaoImpl">
        <property name="dataSource" ref="tmpgarDataSource"/>
        <property name="successQuery" value="${jdbc.successquery}"/>
        <property name="successQueryColumns" value="${jdbc.successquerycolumns}"/>
        <property name="failureQuery" value="${jdbc.failurequery}"/>
        <property name="failureQueryColumns" value="${jdbc.failurequerycolumns}"/>
        <property name="detailedFailureQuery" value="${jdbc.detailedfailurequery}"/>
        <property name="detailedFailureQueryColumns" value="${jdbc.detailedfailurequerycolumns}"/>
    </bean>

</beans>